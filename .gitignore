# 원본 전사본과 그 파생물.
# 금융감독원 공개 녹취와 AI Hub 음성에서 온 자료라 재배포가 허용되지 않는다 —
# 받는 방법은 fetch.py, 조건은 README.md 참고.
*.csv
*.jsonl
*.json
repo/
finetune/

# 드라이브에 올릴 파일만 모아둔 폴더. build_binary_set.py 로 언제든 다시 만든다.
colab_upload/

# 학습 산출물. 체크포인트는 재현 가능하므로 커밋하지 않는다.
# 다만 adapter/calibration.json(온도)은 숫자 하나뿐이고 추론에 필요하니 예외로 둔다.
adapter/*
checkpoint-*/
!adapter/calibration.json
!adapter/prompt.json

__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
.DS_Store
